import { DomainResource } from './DomainResource';

export enum DiscriminatorTypeEnum{
    EXISTS = 'exists',
    PATTERN = 'pattern',
    PROFILE = 'profile',
    TYPE = 'type',
    VALUE = 'value',
}
