import { DomainResource } from './DomainResource';

export enum ResearchSubjectStatusEnum{
    ACTIVE = 'active',
    CANDIDATE = 'candidate',
    COMPLETED = 'completed',
    ENROLLED = 'enrolled',
    SUSPENDED = 'suspended',
    WITHDRAWN = 'withdrawn',
}
