import { DomainResource } from './DomainResource';

export enum SearchModifierCodeEnum{
    ABOVE = 'above',
    BELOW = 'below',
    CONTAINS = 'contains',
    EXACT = 'exact',
    IN = 'in',
    MISSING = 'missing',
    NOT = 'not',
    NOT_IN = 'not-in',
    TEXT = 'text',
    TYPE = 'type',
}
