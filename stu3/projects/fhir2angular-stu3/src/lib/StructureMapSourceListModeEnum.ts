import { DomainResource } from './DomainResource';

export enum StructureMapSourceListModeEnum{
    FIRST = 'first',
    LAST = 'last',
    NOT_FIRST = 'not_first',
    NOT_LAST = 'not_last',
    ONLY_ONE = 'only_one',
}
