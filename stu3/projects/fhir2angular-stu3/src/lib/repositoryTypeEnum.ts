import { DomainResource } from './DomainResource';

export enum repositoryTypeEnum{
    DIRECTLINK = 'directlink',
    LOGIN = 'login',
    OAUTH = 'oauth',
    OPENAPI = 'openapi',
    OTHER = 'other',
}
