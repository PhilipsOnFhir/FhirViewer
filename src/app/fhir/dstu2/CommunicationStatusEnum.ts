import { DomainResource } from './DomainResource'

export enum CommunicationStatusEnum{
    COMPLETED = 'completed',
    FAILED = 'failed',
    IN_PROGRESS = 'in-progress',
    REJECTED = 'rejected',
    SUSPENDED = 'suspended',
}
