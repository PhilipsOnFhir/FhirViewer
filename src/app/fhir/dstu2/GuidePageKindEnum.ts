import { DomainResource } from './DomainResource'

export enum GuidePageKindEnum{
    DICTIONARY = 'dictionary',
    DIRECTORY = 'directory',
    EXAMPLE = 'example',
    INCLUDE = 'include',
    LIST = 'list',
    PAGE = 'page',
    RESOURCE = 'resource',
    TOC = 'toc',
}
