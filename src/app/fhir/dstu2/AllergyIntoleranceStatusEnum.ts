import { DomainResource } from './DomainResource'

export enum AllergyIntoleranceStatusEnum{
    ACTIVE = 'active',
    CONFIRMED = 'confirmed',
    ENTERED_IN_ERROR = 'entered-in-error',
    INACTIVE = 'inactive',
    REFUTED = 'refuted',
    RESOLVED = 'resolved',
    UNCONFIRMED = 'unconfirmed',
}
