import { DomainResource } from './DomainResource'

export enum EpisodeOfCareStatusEnum{
    ACTIVE = 'active',
    CANCELLED = 'cancelled',
    FINISHED = 'finished',
    ONHOLD = 'onhold',
    PLANNED = 'planned',
    WAITLIST = 'waitlist',
}
