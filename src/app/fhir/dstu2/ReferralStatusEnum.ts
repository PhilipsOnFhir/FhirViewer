import { DomainResource } from './DomainResource'

export enum ReferralStatusEnum{
    ACCEPTED = 'accepted',
    ACTIVE = 'active',
    CANCELLED = 'cancelled',
    COMPLETED = 'completed',
    DRAFT = 'draft',
    REJECTED = 'rejected',
    REQUESTED = 'requested',
}
