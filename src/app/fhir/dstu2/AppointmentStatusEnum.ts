import { DomainResource } from './DomainResource'

export enum AppointmentStatusEnum{
    ARRIVED = 'arrived',
    BOOKED = 'booked',
    CANCELLED = 'cancelled',
    FULFILLED = 'fulfilled',
    NOSHOW = 'noshow',
    PENDING = 'pending',
    PROPOSED = 'proposed',
}
