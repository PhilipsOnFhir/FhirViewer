import { DomainResource } from './DomainResource'

export enum SupplyRequestStatusEnum{
    CANCELLED = 'cancelled',
    COMPLETED = 'completed',
    FAILED = 'failed',
    REQUESTED = 'requested',
}
