import { DomainResource } from './DomainResource'

export enum DataElementStringencyEnum{
    COMPARABLE = 'comparable',
    CONVERTABLE = 'convertable',
    EQUIVALENT = 'equivalent',
    FLEXIBLE = 'flexible',
    FULLY_SPECIFIED = 'fully-specified',
    SCALEABLE = 'scaleable',
}
