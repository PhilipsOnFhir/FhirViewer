import { DomainResource } from './DomainResource'

export enum FilterOperatorEnum{
    EQUALS = '=',
    IN = 'in',
    IS_A = 'is-a',
    IS_NOT_A = 'is-not-a',
    NOT_IN = 'not-in',
    REGEX = 'regex',
}
