import { DomainResource } from './DomainResource'

export enum ProvenanceEntityRoleEnum{
    DERIVATION = 'derivation',
    QUOTATION = 'quotation',
    REVISION = 'revision',
    SOURCE = 'source',
}
