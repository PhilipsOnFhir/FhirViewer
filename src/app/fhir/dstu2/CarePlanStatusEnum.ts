import { DomainResource } from './DomainResource'

export enum CarePlanStatusEnum{
    ACTIVE = 'active',
    CANCELLED = 'cancelled',
    COMPLETED = 'completed',
    DRAFT = 'draft',
    PROPOSED = 'proposed',
}
