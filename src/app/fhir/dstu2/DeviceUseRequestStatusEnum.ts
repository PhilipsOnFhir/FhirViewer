import { DomainResource } from './DomainResource'

export enum DeviceUseRequestStatusEnum{
    ABORTED = 'aborted',
    ACCEPTED = 'accepted',
    COMPLETED = 'completed',
    IN_PROGRESS = 'in-progress',
    PLANNED = 'planned',
    PROPOSED = 'proposed',
    RECEIVED = 'received',
    REJECTED = 'rejected',
    REQUESTED = 'requested',
    SUSPENDED = 'suspended',
}
