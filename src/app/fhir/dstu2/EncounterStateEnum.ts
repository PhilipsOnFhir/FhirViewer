import { DomainResource } from './DomainResource'

export enum EncounterStateEnum{
    ARRIVED = 'arrived',
    CANCELLED = 'cancelled',
    FINISHED = 'finished',
    IN_PROGRESS = 'in-progress',
    ONLEAVE = 'onleave',
    PLANNED = 'planned',
}
