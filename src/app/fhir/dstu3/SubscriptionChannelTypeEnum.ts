import { DomainResource } from './DomainResource'

export enum SubscriptionChannelTypeEnum{
    EMAIL = 'email',
    MESSAGE = 'message',
    REST_HOOK = 'rest-hook',
    SMS = 'sms',
    WEBSOCKET = 'websocket',
}
