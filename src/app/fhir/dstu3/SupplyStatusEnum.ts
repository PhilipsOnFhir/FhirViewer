import { DomainResource } from './DomainResource'

export enum SupplyStatusEnum{
    CANCELLED = 'cancelled',
    DISPENSED = 'dispensed',
    FAILED = 'failed',
    RECEIVED = 'received',
    REQUESTED = 'requested',
}
