import { DomainResource } from './DomainResource'

export enum ProtocolStatusEnum{
    ACTIVE = 'active',
    DRAFT = 'draft',
    REVIEW = 'review',
    SUPERSEDED = 'superseded',
    TESTING = 'testing',
    WITHDRAWN = 'withdrawn',
}
