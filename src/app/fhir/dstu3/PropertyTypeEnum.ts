import { DomainResource } from './DomainResource'

export enum PropertyTypeEnum{
    CODING = 'Coding',
    BOOLEAN = 'boolean',
    CODE = 'code',
    DATETIME = 'dateTime',
    INTEGER = 'integer',
    STRING = 'string',
}
