import { DomainResource } from './DomainResource'

export enum ContractResourceStatusCodesEnum{
    AMENDED = 'amended',
    APPENDED = 'appended',
    CANCELLED = 'cancelled',
    DISPUTED = 'disputed',
    ENTERED_IN_ERROR = 'entered-in-error',
    EXECUTABLE = 'executable',
    EXECUTED = 'executed',
    NEGOTIABLE = 'negotiable',
    OFFERED = 'offered',
    POLICY = 'policy',
    REJECTED = 'rejected',
    RENEWED = 'renewed',
    RESOLVED = 'resolved',
    REVOKED = 'revoked',
    TERMINATED = 'terminated',
}
