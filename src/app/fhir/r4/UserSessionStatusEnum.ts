import { DomainResource } from './DomainResource'

export enum UserSessionStatusEnum{
    ACTIVATING = 'activating',
    ACTIVE = 'active',
    CLOSED = 'closed',
    CLOSING = 'closing',
    SUSPENDED = 'suspended',
}
