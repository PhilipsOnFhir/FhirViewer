import { DomainResource } from './DomainResource'

export enum PropertyTypeEnum{
    CODING = 'Coding',
    BOOLEAN = 'boolean',
    CODE = 'code',
    DATETIME = 'dateTime',
    DECIMAL = 'decimal',
    INTEGER = 'integer',
    STRING = 'string',
}
