import { DomainResource } from './DomainResource'

export enum UDIEntryTypeEnum{
    BARCODE = 'barcode',
    CARD = 'card',
    MANUAL = 'manual',
    RFID = 'rfid',
    SELF_REPORTED = 'self-reported',
    UNKNOWN = 'unknown',
}
