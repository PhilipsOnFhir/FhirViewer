import { DomainResource } from './DomainResource'

export enum SearchParamTypeEnum{
    COMPOSITE = 'composite',
    DATE = 'date',
    NUMBER = 'number',
    QUANTITY = 'quantity',
    REFERENCE = 'reference',
    STRING = 'string',
    TOKEN = 'token',
    URI = 'uri',
}
